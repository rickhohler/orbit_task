# orbit_task_generator

Code generator for the `orbit_task` plugin.

## Usage

This package works in conjunction with `orbit_task_annotations` to generate the necessary boilerplate for registering background task callbacks.

Run the build runner to generate code:

```bash
flutter pub run build_runner build
```
